export const chains = [];

export const relayers = [];

// Goals
// - Run multiple blockchains with hyperlane deployed
// - Connect blockchains via hyperlane
// - Relay messages (run a relayer)
// - Validate messages (run validators for each chain)
//    - Maybe support trusted relayer to begin with
// - Run observability so we can dev dashboards and alerts
// - Run custom things like solana register module (custom zeta code)
